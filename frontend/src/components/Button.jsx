import React from "react";
import { Link } from "react-router-dom";

const STYLES = {
  solid:
    "border border-[#2a2a2a] py-2 sm:py-3 px-8 sm:px-10 rounded-full sm:text-lg text-sm font-semibold tracking-wider transition-all duration-300 hover:bg-[#1a1a1a] bg-gray-300 text-black hover:text-white",
    outline: "border border-[#2a2a2a] py-2 sm:py-3 px-4 sm:px-5 rounded-full sm:text-lg text-sm font-semibold tracking-wider transition-all duration-300 hover:bg-[#1a1a1a]"
};

const Button = ({ children, onClick, style, size }) => {
  return (
    <Link>
    </Link>
  );
};

export default Button;
